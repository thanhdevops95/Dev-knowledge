# Placeholder — file ĐÍCH THỰC sẽ do `kubeseal` sinh ra từ Secret plaintext.
# Encrypted blob bên dưới chỉ minh hoạ; CHÚ Ý: encryptedData phải sinh từ chính cluster của bạn
# (kubeseal lấy public key từ controller trong cluster đó), không copy được từ máy khác sang.
#
# Cách tạo file thật:
#   kubectl create secret generic myapp-secret \
#     --from-literal=DB_PASSWORD=supersecret \
#     --dry-run=client -o yaml > secret-plain.yaml
#   kubeseal --format yaml < secret-plain.yaml > sealedsecret-example.yaml
apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  name: myapp-secret
  namespace: myapp-dev
spec:
  encryptedData:
    DB_PASSWORD: AgB...REPLACE_BY_KUBESEAL_OUTPUT...==
  template:
    metadata:
      name: myapp-secret
      namespace: myapp-dev
    type: Opaque
