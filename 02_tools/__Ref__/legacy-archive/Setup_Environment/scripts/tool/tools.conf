# Format: Display Name;Type;Brew Name;Apt Name
# Type can be 'cli' or 'cask' (for macOS). Leave Apt Name blank if not applicable.
#
# --- GUI Applications (macOS First) ---
Visual Studio Code;cask;visual-studio-code;code
Docker Desktop;cask;docker;
iTerm2;cask;iterm2;
Rectangle;cask;rectangle;
#
# --- CLI Tools (Cross-Platform) ---
Git;cli;git;git
Python 3;cli;python3;python3
Kubernetes (kubectl);cli;kubectl;kubectl
Minikube;cli;minikube;minikube
Terraform;cli;terraform;terraform
Ansible;cli;ansible;ansible
AWS CLI;cli;awscli;awscli
Helm;cli;helm;helm
k9s;cli;k9s;k9s
Gitleaks;cli;gitleaks;gitleaks
pre-commit;cli;pre-commit;pre-commit
direnv;cli;direnv;direnv
Wget;cli;wget;wget
Curl;cli;curl;curl
jq;cli;jq;jq
nmap;cli;nmap;nmap
htop;cli;htop;htop
telnet;cli;telnet;telnet
#
# --- New Tools ---
lazydocker;cli;lazydocker;lazydocker
packer;cli;packer;packer
trivy;cli;trivy;trivy
gum;cli;gum;gum
