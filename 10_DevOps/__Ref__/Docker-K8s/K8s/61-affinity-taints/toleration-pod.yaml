apiVersion: v1
kind: Pod
metadata:
  name: toleration-demo
  namespace: myapp-dev
spec:
  tolerations:
    - key: "dedicated"
      operator: "Equal"
      value: "gpu"
      effect: "NoSchedule"
  containers:
    - name: app
      image: busybox:1.36
      command: ["sh", "-c", "sleep 3600"]
